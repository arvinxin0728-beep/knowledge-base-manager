interface:
  display_name: "Knowledge Base Manager"
  short_description: "Manage mapped AI knowledge-base workflows"
  default_prompt: "Use $knowledge-base-manager to initialize, audit, process, and output from my mapped knowledge base."

policy:
  allow_implicit_invocation: true
